# Перевірка відновлення з GitHub — 24.09.2026

Перевірено ревізію `1280e7d1aec64e5834ebca0ca8003bd81daadef7` гілки
`codex/checkbox-marketplace-orders` репозиторію https://github.com/Pvitaly91/CRMapp.
Код застосунку відповідає `6ca345b`; наступні зміни стосуються лише інструкції та скрипта збірки.

## Що реально виконано

- Створено окремий чистий клон безпосередньо з GitHub. Локальні `bin`, `obj`, вихідні файли чи ресурси з робочої копії не копіювалися.
- Запущено `powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1` у Windows із .NET SDK 8.0.425.
- Release: успіх, 0 помилок, 0 попереджень.
- Консольний набір регресій: **130/130 PASS**, включно з Windows/WPF/STA-сценаріями.
- Self-contained `win-x64`: успіх, створено EXE і супровідні файли .NET.
- Повторний запуск у непорожню папку завершився очікуваною відмовою до збірки: готові файли не перезаписуються.
- Робоче дерево контрольного клону після перевірки залишилося чистим. Використовувався встановлений SDK/стандартний кеш .NET; це перевірка чистого клону, не окрема свіжа інсталяція Windows.

## Прибирання локальних білдів

24 старі опубліковані збірки переміщено у кошик Windows: 20 із
`D:\Portable Soft\POS\CRMapp\artifacts` та 4 із `D:\DEV\CPP\checkbox\artifacts`.
Залишено `D:\Portable Soft\POS\CRMapp\artifacts\win-x64-no-duplicates`.
Контрольний хеш його EXE до/після прибирання збігається.
Вихідні репозиторії, налаштування, секрети, кеш і історію друку не видаляли.

## Межі перевірки

Реальні API та фізичний друк у цій перевірці не запускалися. Для фізичної перевірки потрібні
локально налаштовані облікові дані, встановлений драйвер і підтвердження користувача про вихід паперу.
Збірка з GitHub відновлює програму, але не приватні дані користувача з `%LOCALAPPDATA%\CheckboxBatchPrinter`.

Команди відновлення наведено у [README](../README.md#збірка-та-тести).
